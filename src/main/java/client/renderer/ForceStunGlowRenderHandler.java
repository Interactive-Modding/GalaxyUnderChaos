package client.renderer;

/**
 * Disabled compatibility stub.
 *
 * Force Stun was reverted to the original separate ForceAbilityEffectEntity
 * cage/ring visual. Keeping this class as a no-op safely overwrites the
 * temporary model-shell event subscriber without requiring manual deletion.
 */
public final class ForceStunGlowRenderHandler {
    private ForceStunGlowRenderHandler() {
    }
}
